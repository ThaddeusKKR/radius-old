const { MessageEmbed } = require('discord.js')
const fs = require('fs')

module.exports = {
    name: 'write',
    description: 'Writes to a JSON file.',
    aliases: [],
    ownerOnly: true,
    async execute(message, args) {
       
    }
}