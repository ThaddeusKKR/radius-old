const { MessageEmbed } = require('discord.js')
const Keyv = require('keyv')

module.exports = {
    name: 'info',
    description: 'Shows info about a track in the queue',
    aliases: [],
    category: '',
    modOnly: false,
    ownerOnly: false,
    async execute(message, args, prefix) {

    }
}